apiVersion: v1
kind: Service
metadata:
  name: echo
  namespace: istio-poc
  labels:
    app: echo
spec:
  type: ClusterIP
  selector:
    app: echo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
